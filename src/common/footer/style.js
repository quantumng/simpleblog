import styled from 'styled-components'

export const FooterWrapper = styled.div`
  width: 100%;
  height:30px;
  overflow: auto;
  text-align: center;
  font-size: 12px;
  color: rgba(0,0,0,.5);
  a {
    color: rgba(0,0,0,.5);
  }
`